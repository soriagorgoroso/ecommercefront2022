export function userReducer(state = []) {
  return state;
}

export function articleReducer(state = []) {
  return state;
}

export function orderReducer(state = []) {
  return state;
}

export function categoryReducer(state = []) {
  return state;
}
